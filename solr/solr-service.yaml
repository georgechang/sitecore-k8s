apiVersion: v1
kind: Service
metadata:
  name: solr
  labels:
    app: solr
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
    service.beta.kubernetes.io/azure-load-balancer-internal-subnet: "solr-subnet"
spec:
  type: LoadBalancer
  ports:
    - port: 8983
  selector:
    app: solr
